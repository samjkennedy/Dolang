

let putc char -> (print)

let puts string -> (print)

let chareq char char -> bool (=)
let any bool array -> bool (
    false (or) fold
)

let chardup char -> char char (dup)

let main -> (
    "Hello " "world" concat print
    "Hello"
        (putc) foreach

    "Hello" ('e chareq) map
        any print

    "Hello" len print

    "Characters On uppercase OnLy"
        (chardup 'A >= swap 'Z <= and) filter
        print
)