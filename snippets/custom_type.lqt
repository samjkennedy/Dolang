type Point {
    x: int
    y: int
}

let square int int -> int (dup *)
let length p: Point -> int (
    p.x square p.y square + isqrt
)

let main -> (
    3 4 Point 
    set p (
        p print
        p length print
        p.x print
        p.y print
    )
)